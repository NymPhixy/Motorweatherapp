[build]
  command = "npm --prefix motor-weather-app install; npm --prefix motor-weather-app run build"
  publish = "motor-weather-app/dist"

# Keep SPA routes working on refresh/direct link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
